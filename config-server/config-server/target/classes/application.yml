server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cosmologysk8/bootcamp-microservicios.git
        default-label: main
        search-paths:
        - '*service*'